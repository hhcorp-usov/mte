@model IEnumerable<mte.Models.Smenes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ControlerEmployers.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DispEmployers.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SmenaDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ControlerEmployers.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DispEmployers.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SmenaDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
